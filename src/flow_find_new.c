#include "lem_in.h"

t_list			*flow_find_new(t_wrap *wrap, t_node *source, t_node *sink)
{
	// look at all nodes from each node in current frontier
	(void)wrap;
	(void)source;
	(void)sink;
	return (0);
}
